---
slug: /aiagent-server/advanced/round-mechanism-and-callback-tracking
title: Round Mechanism and Callback Tracking
---




# Round Mechanism and Callback Tracking

## Overview

**Round is the unique identifier for AI Agent interaction flow.** For each user speech or API call, the server generates a Round value. All subsequent related callbacks (ASR recognition, LLM response, TTS playback, status changes, interruption events, etc.) carry this Round value, enabling the business side to accurately track "what happened in this interaction".

**Applicable scenarios:** AI chat, voice chat rooms, digital humans, intelligent customer service, and other scenarios that require tracking complete conversation flows. Whether the user speaks proactively or the business side triggers AI responses, Round can link all callbacks together to easily handle complex situations like interruptions and queuing.

---

## Core Concept: What is Round

### Definition of Round

**Round** is an ascending sequence number generated by the server for each complete interaction. It never repeats.

- **One interaction** = Complete flow from "trigger" to "end"
- **Trigger sources**: User speech, calling sendAgentInstanceLLM, calling sendAgentInstanceTTS
- **End marker**: AI completes the current round response (TTS playback finished or LLM response completed)

### Purpose of Round

Round spans the entire interaction lifecycle. All callback events carry the Round field:

```plaintext
Trigger → Round N starts
  ├─ ASRResult (Round: N)        // ASR starts recognition result
  ├─ LLMResult (Round: N)        // LLM response content
  ├─ AgentInstanceStatus (Round: N)  // Status change (Listening→Thinking→Speaking→Idle)
  ├─ AgentInstanceMetaInfo (Round: N) // Metadata (voice, emotion, etc.)
  └─ End → Round N completed
```

### When Round is Generated

| Trigger Source | Round Generation Timing | Description |
| --- | --- | --- |
| User speaks | User voice detected | Round is determined when ASR starts recognition |
| sendAgentInstanceLLM | API request succeeds | Server assigns Round immediately upon receiving the request |
| sendAgentInstanceTTS | API request succeeds | Server assigns Round immediately upon receiving the request |

### Client SDK Callback (Getting Round)

**Important:** Clients can obtain Round information through **ZEGO Express SDK experimental API** callbacks, used to implement status switch UI (e.g., "Listening", "Thinking", "Speaking").

#### Message Format (General)

```json
{
  "Timestamp": 1765510379,           // Second-level timestamp
  "TimestampMs": 1765510379113,      // Millisecond-level timestamp
  "SeqId": 278800715,                // Packet sequence number, guarantees ordering, not consecutive
  "Round": 510359002,                // 【Core】Conversation round, generated in ascending order, not consecutive
  "Cmd": 1,                          // Command type
  "Data": { ... }                    // Specific content, see table below
}
```

#### Cmd Types and Data Fields

| Cmd | 类型 | Data 字段 | 说明 |
| --- | --- | --- | --- |
| 1 | User speech status | SpeakStatus, UserId | SpeakStatus: 1=speech starts, 2=speech ends |
| 3 | ASR text | Text, MessageId, UserId, StartFlag, EndFlag | Incrementally delivers ASR recognition results |
| 4 | LLM response | Text, MessageId, EndFlag | Incrementally delivers LLM responses |
| 6 | Agent status | Status, OldStatus, Reason | Status: 0=Idle, 1=Listening, 2=Thinking, 3=Speaking |
| 102 | Metadata info | Object | Metadata info (voice, emotion, action, etc.) |

#### Detailed Documentation

For complete API documentation, refer to: [智能体实例 SDK 端回调](https://doc-zh.zego.im/aiagent-android/client-sdk/ai-related-callback)

---

## Diagrams

### Data Flow Overview

```plaintext
┌─────────────────────────────────────────────────────────────────┐
│                        Trigger Sources                          │
├───────────────────────┬─────────────────────────────────────────┤
│   User speaks proactively   │   Business side calls API             │
│   (RTC room voice)          │   sendAgentInstanceLLM/TTS              │
└───────────┬───────────┴────────────────┬────────────────────────┘
            │                            │
            ▼                            ▼
    ┌────────────────────────────────────────┐
    │    ZEGO AI Agent Server                       │
    │    Generate Round (ascending sequence)       │
    └────────────────────────────────────────┘
                    │
        ┌───────────┼───────────┬───────────┐
        │           │           │           │
        ▼           ▼           ▼           ▼
  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
  │   ASR    │ │   LLM    │ │   TTS    │ │  Status  │
  │  Result  │ │  Result  │ │  Status  │ │  Change  │
  │ Round: N │ │ Round: N │ │ Round: N │ │ Round: N │
  └──────────┘ └──────────┘ └──────────┘ └──────────┘
        │           │           │           │
        └───────────┴───────────┴───────────┘
                    │
                    ▼
        ┌───────────────────────┐
        │   Business Server Callbacks                  │
        │   Correlate all events through Round        │
        └───────────────────────┘
```

### Comparison of Two Trigger Sources

```plaintext
【场景 1：用户主动说话】
User: "How's the weather today?" (Round 5)
  ↓
Server: ASR recognition → LLM thinking → TTS playback
  ↓
回调序列：
  - ASRResult (Round: 5, text: "How's the weather today?")
  - AgentInstanceStatus (Round: 5, status: "Thinking")
  - LLMResult (Round: 5, text: "The weather is sunny..."  )
  - AgentInstanceStatus (Round: 5, status: "Speaking")
  - AgentInstanceStatus (Round: 5, status: "Idle")

【场景 2：业务方调用 API】
Business: sendAgentInstanceTTS("Welcome") (Round 6)
  ↓
Server: Direct TTS playback
  ↓
回调序列：
  - AgentInstanceStatus (Round: 6, status: "Speaking")
  - AgentInstanceMetaInfo (Round: 6, voice: "zh_female")
  - AgentInstanceStatus (Round: 6, status: "Idle")
```

---

## Callback Events Overview

### Callback Type List

| Callback Event | Has Round | Trigger Timing | Purpose |
| --- | --- | --- | --- |
| ASRResult | ✅ | User speech recognition completed | Get ASR recognition text |
| LLMResult | ✅ | LLM generates response | Get AI response content |
| AgentInstanceStatus | ✅ | Status change | Track AI current status (Listening/Thinking/Speaking/Idle) |
| AgentInstanceMetaInfo | ✅ | Broadcast starts | Get metadata (voice, emotion, action, etc.) |
| Interrupted | ✅ | Interrupted | Handle interruption logic |
| UserSpeakAction | ✅ | User starts/stops speaking | Detect user speech |
| AgentSpeakAction | ✅ | AI starts/stops speaking | Detect AI speech |
| Exception | ✅ | Error occurred | Error handling |
| UserAudioData | ✅ | User speech audio data | Get user voice PCM data |

### Callback Field Description (Key Fields)

Common fields for all callback events (Round is inside the Data structure):

```json
{
  "Event": "ASRResult",           // Event type
  "RoomId": "room_123",               // Room ID
  "AgentUserId": "agent_user_001",    // Agent user ID
  "Sequence": 1234567890,             // Event sequence number (globally incrementing)
  "Timestamp": 1746619200000,         // Timestamp (milliseconds)
  "Data": { "Round": 5, ... }         // Event specific data, Round is inside Data
}
```

---

## Usage Examples

### Case 1: User Question

#### Flow Sequence Diagram

```plaintext
时间线 →
  ├─ T0: 用户开始说话
  ├─ T1: ASR 识别完成 → ASRResult (Round: 1)
  ├─ T2: AI 开始思考 → AgentInstanceStatus (Round: 1, Status: "Thinking")
  ├─ T3: LLM 生成回复 → LLMResult (Round: 1)
  ├─ T4: AI 开始播报 → AgentInstanceStatus (Round: 1, Status: "Speaking")
  ├─ T5: 播报开始回调 → AgentInstanceMetaInfo (Round: 1)
  └─ T6: 播报完成 → AgentInstanceStatus (Round: 1, Status: "Idle")
```

#### Server Callback Sequence (Chronological)

| 时间 | 事件类型 | Round | 关键数据 |
| --- | --- | --- | --- |
| T1 | ASRResult | 1 | Result: "Hello, introduce yourself" |
| T2 | AgentInstanceStatus | 1 | Status: "Thinking" |
| T3 | LLMResult | 1 | Content: "Hello, I am an AI assistant..." |
| T4 | AgentInstanceStatus | 1 | Status: "Speaking" |
| T5 | AgentInstanceMetaInfo | 1 | MetaInfo: `{"VoiceId": "zh_female"}` |
| T6 | AgentInstanceStatus | 1 | Status: "Idle" |

#### Client SDK Callback Sequence (Chronological)

| 时间 | Cmd | Round | 关键数据 |
| --- | --- | --- | --- |
| T0 | Cmd=1 | 1 | SpeakStatus: 1 (user starts speaking) |
| T1 | Cmd=3 | 1 | Text: "Hello, introduce yourself", EndFlag: true |
| T2 | Cmd=6 | 1 | Status: 2 (Thinking) |
| T3 | Cmd=4 | 1 | Text: "Hello, I am an AI assistant...", EndFlag: true |
| T4 | Cmd=6 | 1 | Status: 3 (Speaking) |
| T5 | Cmd=102 | 1 | MetaInfo: `{"VoiceId": "zh_female"}` |
| T6 | Cmd=6 | 1 | Status: 0 (Idle) |

#### API Request Example（无，用户主动触发）

#### Callback Example

**ASRResult**

```json
{
  "Event": "ASRResult",
  "Data": {
    "Round": 1,
    "Result": "Hello, introduce yourself",
    "IsFinal": true
  }
}
```

**LLMResult**

```json
{
  "Event": "LLMResult",
  "Data": {
    "Round": 1,
    "Content": "Hello, I am an AI assistant, happy to serve you."
  }
}
```

**AgentInstanceStatus**

```json
{
  "Event": "AgentInstanceStatus",
  "Data": {
    "Round": 1,
    "Status": "Speaking"  // Thinking | Speaking | Idle | Listening
  }
}
```

**Client SDK Callback Example (Cmd=3, ASR Text)**

```json
{
  "Timestamp": 1765510379,
  "TimestampMs": 1765510379113,
  "SeqId": 278800715,
  "Round": 1,
  "Cmd": 3,
  "Data": {
    "Text": "你好，介绍一下你自己",
    "MessageId": "msg_001",
    "UserId": "user_123",
    "StartFlag": true,
    "EndFlag": true
  }
}
```

---

### Case 2: sendAgentInstanceLLM

#### Flow Sequence Diagram

```plaintext
Business calls sendAgentInstanceLLM
         │
         ▼
   Server generates Round: 2
         │
         ▼
   LLM starts generating → AgentInstanceStatus (Round: 2, Status: "Thinking")
         │
         ▼
   LLM generation complete → LLMResult (Round: 2)
         │
         ▼
   TTS starts playback → AgentInstanceStatus (Round: 2, Status: "Speaking")
         │
         ▼
   Playback complete → AgentInstanceStatus (Round: 2, Status: "Idle")
```

#### API Request Example

```json
POST https://aigc-aiagent-api.zegotech.cn/
{
  "Action": "sendAgentInstanceLLM",
  "AppId": 1234567890,
  "AgentId": "agent_001",
  "AgentUserId": "agent_user_001",
  "Text": "Tell me a joke",
  "AddHistory": true
}
```

#### Server Callback Sequence

| 时间 | 事件类型 | Round | 关键数据 |
| --- | --- | --- | --- |
| T1 | AgentInstanceStatus | 2 | Status: "Thinking" |
| T2 | LLMResult | 2 | Content: "One day, 0 met 8..." |
| T3 | AgentInstanceStatus | 2 | Status: "Speaking" |
| T4 | AgentInstanceMetaInfo | 2 | MetaInfo: `{"VoiceId": "zh_female"}` |
| T5 | AgentInstanceStatus | 2 | Status: "Idle" |

#### Client SDK Callback Sequence

| 时间 | Cmd | Round | 关键数据 |
| --- | --- | --- | --- |
| T1 | Cmd=6 | 2 | Status: 2 (Thinking) |
| T2 | Cmd=4 | 2 | Text: "One day, 0 met 8...", EndFlag: true |
| T3 | Cmd=6 | 2 | Status: 3 (Speaking) |
| T4 | Cmd=102 | 2 | MetaInfo: `{"VoiceId": "zh_female"}` |
| T5 | Cmd=6 | 2 | Status: 0 (Idle) |

#### Callback Example

**LLMResult**

```json
{
  "Event": "LLMResult",
  "Data": {
    "Round": 2,
    "Content": "One day, 0 met 8. 0 said disdainfully: 'So what if I'm fat, why wear a belt!'"
  }
}
```

---

### Case 3: sendAgentInstanceTTS

#### Flow Sequence Diagram

```plaintext
Business calls sendAgentInstanceTTS
         │
         ▼
   Server generates Round: 3
         │
         ▼
   TTS starts playback → AgentInstanceStatus (Round: 3, Status: "Speaking")
         │
         ▼
   Playback start callback → AgentInstanceMetaInfo (Round: 3)
         │
         ▼
   Playback complete → AgentInstanceStatus (Round: 3, Status: "Idle")
```

#### API Request Example

```json
POST https://aigc-aiagent-api.zegotech.cn/
{
  "Action": "sendAgentInstanceTTS",
  "AppId": 1234567890,
  "AgentId": "agent_001",
  "AgentUserId": "agent_user_001",
  "Text": "Welcome: Nice to meet you!",
  "AddHistory": false
}
```

#### Server Callback Sequence

| 时间 | 事件类型 | Round | 关键数据 |
| --- | --- | --- | --- |
| T1 | AgentInstanceStatus | 3 | Status: "Speaking" |
| T2 | AgentInstanceMetaInfo | 3 | MetaInfo: `{"VoiceId": "zh_female"}` |
| T3 | AgentInstanceStatus | 3 | Status: "Idle" |

#### Client SDK Callback Sequence

| 时间 | Cmd | Round | 关键数据 |
| --- | --- | --- | --- |
| T1 | Cmd=6 | 3 | Status: 3 (Speaking) |
| T2 | Cmd=102 | 3 | MetaInfo: `{"VoiceId": "zh_female"}` |
| T3 | Cmd=6 | 3 | Status: 0 (Idle) |

#### Callback Example

**AgentInstanceMetaInfo**

```json
{
  "Event": "AgentInstanceMetaInfo",
  "Data": {
    "Round": 3,
    "MetaInfo": {
      "VoiceId": "zh_female",
      "Speed": 1.0
    }
  }
}
```

---

## Edge Case Handling

### Interruption Scenario

**Scenario description:** AI is broadcasting Round 3 content, and the user suddenly speaks to interrupt.

#### Server Callback Sequence

| 时间 | 事件类型 | Round | 说明 |
| --- | --- | --- | --- |
| T1 | AgentInstanceStatus | 3 | Status: "Speaking" (AI broadcasting) |
| T2 | UserSpeakAction | 4 | User speech detected, **new Round 4 starts** |
| T3 | Interrupted | 3 | **Round 3 interrupted** |
| T4 | AgentInstanceStatus | 3 | Status: "Idle" (Round 3 terminated) |
| T5 | ASRResult | 4 | User speech recognition result |
| T6 | AgentInstanceStatus | 4 | Status: "Thinking" (processing Round 4) |

#### Client SDK Callback Sequence

| 时间 | Cmd | Round | 说明 |
| --- | --- | --- | --- |
| T1 | Cmd=6 | 3 | Status: 3 (Speaking) |
| T2 | Cmd=1 | 4 | SpeakStatus: 1 (user starts speaking) |
| T3 | Cmd=6 | 3 | Status: 0 (Idle, Round 3 terminated) |
| T4 | Cmd=3 | 4 | Text: "User speech content", EndFlag: true |
| T5 | Cmd=6 | 4 | Status: 2 (Thinking) |

#### Key Logic

- When interruption occurs, **current Round terminates immediately**, status becomes Idle
- User speech triggers a **new Round**, processing continues from the new Round
- Business side should listen to `Interrupted` events and clean up the pending state of the interrupted Round

---

### Queuing Scenario

**Scenario description:** Business side calls sendAgentInstanceTTS 3 times consecutively, AI broadcasts in order.

#### Server Callback Sequence

| 时间 | 事件类型 | Round | 说明 |
| --- | --- | --- | --- |
| T1 | sendAgentInstanceTTS (request 1) | 4 | Round 4 assigned |
| T2 | sendAgentInstanceTTS (request 2) | 5 | Round 5 assigned, **queued** |
| T3 | sendAgentInstanceTTS (request 3) | 6 | Round 6 assigned, **queued** |
| T4 | AgentInstanceStatus | 4 | Status: "Speaking" (broadcasting Round 4) |
| T5 | AgentInstanceStatus | 5 | Status: "Speaking" (broadcasting Round 5, **status unchanged, Round changes**) |
| T6 | AgentInstanceStatus | 6 | Status: "Speaking" (broadcasting Round 6, **status unchanged, Round changes**) |
| T7 | AgentInstanceStatus | 6 | Status: "Idle" (all completed) |

#### Client SDK Callback Sequence

| 时间 | Cmd | Round | 说明 |
| --- | --- | --- | --- |
| T4 | Cmd=6 | 4 | Status: 3 (Speaking) |
| T5 | Cmd=6 | 5 | Status: 3 (Speaking, **status unchanged, Round changes**) |
| T6 | Cmd=6 | 6 | Status: 3 (Speaking, **status unchanged, Round changes**) |
| T7 | Cmd=6 | 6 | Status: 0 (Idle, all completed) |

#### Key Logic

- Each API request **immediately assigns a Round**, even if queuing is needed
- During queuing, **AI maintains Speaking status**, Round changes in ascending order
- Business side can track the playback progress of each request through Round

---

## FAQ

### Q1: When is Round generated?

**A:** Generated each time a new interaction is triggered, including:

- User starts speaking
- Calling sendAgentInstanceLLM
- Calling sendAgentInstanceTTS

In interruption scenarios, new interactions trigger new Rounds, old Rounds terminate.

### Q2: Will Round repeat?

**A:** No. Round is generated in ascending order and never repeats.

### Q3: How to debug Round correlation issues?

**A:** It is recommended that the business side logs the Round value of each callback and views them grouped by Round:

```plaintext
[Round 5] ASRResult: "用户提问内容"
[Round 5] LLMResult: "AI 回复内容"
[Round 5] Status: Speaking → Idle
```

If Round is not continuous or events are missing, check:

1. Whether callback configuration is enabled (CallbackConfig)
2. Whether the network is normal (whether callbacks are lost)

### Q4: What is the difference between Round and Sequence?

**A:**

- **Round**: Identifies "which interaction", all events of the same interaction have the same Round
- **Sequence**: Identifies "global order of events", all events strictly increment

Business side uses Round to correlate events of the same interaction, and uses Sequence to detect missing events.

### Q5: Will the interrupted Round still receive LLMResult?

**A:** No. When interrupted, if LLM is still generating, the server stops generation and **will not callback LLMResult**.

Business side should clean up the pending state of the interrupted Round after receiving the `Interrupted` event.

---

## Appendix: Callback Configuration Example

When creating an Agent instance, enable relevant callbacks:

```json
{
  "Action": "CreateAgentInstance",
  "AppId": 1234567890,
  "AgentId": "agent_001",
  "UserId": "user_001",
  "RTC": {
    "RoomId": "room_123",
    "AgentStreamId": "stream_001",
    "AgentUserId": "agent_user_001",
    "UserStreamId": "user_stream_001"
  },
  "CallbackConfig": {
    "ASRResult": 1,              // Enable ASR callback
    "LLMResult": 1,              // Enable LLM callback
    "Interrupted": 1,            // Enable interruption callback
    "UserSpeakAction": 1,        // Enable user speech callback
    "AgentSpeakAction": 1,       // Enable AI speech callback
    "AgentInstanceStatus": 1     // Enable status callback
  }
}
```
